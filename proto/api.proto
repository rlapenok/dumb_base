syntax = "proto3";

package api;

option go_package="github.com/rlapenok/dumb_base/grpc_generate";

message Req {
    uint32 flag=1;
}
message Resp {
        string keys=1;
}

service Api {
        rpc GetKeys(Req) returns (Resp) {}
}
